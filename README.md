A simple expense tracker built with Flask and SQLite. It supports adding, deleting, listing, summarizing, and exporting expenses to CSV.
How to run
pip install -r requirements.txt
python app.py

Open:http://127.0.0.1:5000


